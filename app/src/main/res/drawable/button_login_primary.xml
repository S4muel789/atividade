<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado Pressionado -->
    <item android:state_pressed="true">
        <shape>
            <gradient
                android:startColor="@color/primary_color_dark"
                android:endColor="@color/primary_color_dak"
                android:angle="45" />
            <corners android:radius="12dp" />
            <stroke
                android:width="2dp"
                android:color="@color/primary_color_dak" />
        </shape>
    </item>

    <!-- Estado Desabilitado -->
    <item android:state_enabled="false">
        <shape>
            <solid android:color="#CCCCCC" />
            <corners android:radius="12dp" />
        </shape>
    </item>

    <!-- Estado Normal -->
    <item>
        <shape>
            <gradient
                android:startColor="@color/primary_color_dak"
                android:endColor="@color/primary_color_dark"
                android:angle="45" />
            <corners android:radius="12dp" />
            <stroke
                android:width="1dp"
                android:color="@color/primary_color_dark" />
        </shape>
    </item>
</selector>
